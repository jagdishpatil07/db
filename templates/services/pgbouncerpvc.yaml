apiVersion: v1
kind: Service
metadata:
  name: pgbouncer1-app
  namespace: {{ .Values.global.namespace }}

spec:
  selector:
    app: pgbouncer1-app

  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP

  type: ClusterIP